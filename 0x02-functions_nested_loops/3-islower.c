#include "main.h"

/**
 * _islower - checks for lowercase character
 * @c : input creo
 * Return: 1 if lowercase, 0 otherwise
 */
int _islower(int c)
{

if (c >= 'a')
{
return (1);
}
else
{
return (0);
}
}
